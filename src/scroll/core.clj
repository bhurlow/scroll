(ns scroll.core
  (:require [scroll.db :as db]
            [clj-leveldb :as lev]))

; (use 'less.awful.ssl)
; (def d (partial str "/Users/pretzel/.docker/machine/machines/default"))
; (def files ["key.pem"])
